import { UserInRoleDirective } from './user-in-role.directive';

describe('IfInRoleDirective', () => {
  it('should create an instance', () => {
    // const directive = new UserInRoleDirective();
    // expecKt(directive).toBeTruthy();
  });
});
