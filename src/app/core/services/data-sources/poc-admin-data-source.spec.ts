import { PocAdminDataSource } from './poc-admin-data-source';

describe('PocAdminDataSource', () => {
  it('should create an instance', () => {
    // expect(new PocAdminDataSource()).toBeTruthy();
  });
});
