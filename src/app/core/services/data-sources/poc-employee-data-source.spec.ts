import { PocEmployeeDataSource } from './poc-employee-data-source';

describe('PocEmployeeDataSource', () => {
  it('should create an instance', () => {
    expect(new PocEmployeeDataSource()).toBeTruthy();
  });
});
