export interface IPocAddons {
    userRealRoleId?: string; // UUID
    userRealmGroupId?: string; // UUID
    deviceRealmRoleId?: string; // UUID
    deviceRealmGroupId?: string; // UUID
    deviceId: string; // UUID
    clientCertFolder: string;
}
